import { UploadedMusic } from "@/components/music";

export default function Top() {
  /*
  for (var i = 0; i < 12; i++) {
    musicList.push(new Music((Math.random() * 10000).toString(), "タイトル", "ref", [""], [""], new Date()));
  }*/
  return <UploadedMusic></UploadedMusic>;
}
